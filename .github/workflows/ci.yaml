name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  ci:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - name: Calq Flow (dry-run)
        uses: calq-framework/flow@latest
        with:
          subcommand: 'publish --dry-run'
        env: ${{ secrets }}
